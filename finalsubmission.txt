TASK Final submission. (deadline August 1 evening)
Submit your complete code as a .zip archive together with your trained model weights, runnable through the provided inference interface. 
We must be able to reproduce your reported results from this code and a README. 
The README must explain how to set up the environment and how to obtain and preprocess the data. 
If you collect images from the internet, a list of source URLs is sufficient. 
Develop your project in a GitHub repository and grant us access to it after the submission. 
Include the GitHub repository link in your final report.
 In addition, submit a final report of 8 pages, double column,
excluding references, written in the provided template and following a scientific paper structure with the sections Introduction, Related Work, Method, Experiments, and Conclusions. 
The Experiments section shall include the results of your empirical analyses, evaluations, 
and the explainable AI (XAI) analysis. 
Finally, please add a detailed contributions list in the appendix stating what each member did. 

Your grade does not depend on the raw recognition performance you reach on the held-out test set. 
It reflects the quality of your work and your development process: 
the ideas you explore, your motivation, the soundness of your execution 
and the depth of your empirical analysis. 
Approaches that you tried and that did not improve your results are equally valuable, 
as long as you analyse and discuss them.


============================

Planned improvements before the final packaging:

Priority 0 - make the official inference package runnable:
1. Fix AnimalRecognitionChallenge/inference.py before any further evaluation.
   - Remove the undefined comp_class/comp_conf debug print, the random fallback, TODO comments, and one-image df.sample(n=1) debug path.
   - Resolve detector and checkpoint paths relative to the repository/script, not the caller's current working directory.
   - Fail immediately with a clear error if either classifier checkpoint is missing; never continue with randomly initialized weights.
   - Keep the required Model.forward(PIL.Image) -> {-1, 0, ..., 19} interface unchanged.
   Acceptance check: run the complete provided image folder, process every row of labels.csv, and obtain only valid integer outputs without an exception.
2. Add the final cat_scratch.pth and dog_scratch.pth files to the submission package. They are not present in the current repository, while inference.py requires them.
   - Confirm that both checkpoints match the current EfficientNetV2-S architecture, including the 10-class head and p=0.5 dropout block.
   - Record each file's size and SHA-256 hash in a release manifest.
   Acceptance check: both checkpoints load with strict state-dict checking and produce non-random predictions through the official interface.
3. Validate the detector integration from a clean environment.
   - Verify the included yolov6s.pt and the torch.hub YOLOv6 dependency without relying on an existing local hub cache or interactive trust prompt.
   - Confirm the actual detector output format and remove the unsafe five-value fallback that silently treats an unknown class as a cat unless this format is demonstrated by a test.
   - Add tests for no detection, one cat, one dog, multiple animals, edge-touching boxes, and invalid/empty crops.
   Acceptance check: the detector consistently selects the largest valid cat/dog box, masks secondary detections as intended, crops within image bounds, resizes to 224 x 224, and returns "neither" only under documented conditions.

Priority 1 - freeze one reproducible training and evaluation protocol:
4. Designate one final training path and clearly mark all other scripts/notebooks as experimental or archival.
   - The current submission-facing candidate is Final_Project/train.py; traineffnet.py, the two notebooks, train_PERFEKT_BACKUP.py, and older XAI/comparison helpers describe different experiments and must not be confused with the final recipe.
   - Record the exact command, random seed, hardware, software versions, epoch limit, early-stopping patience, batch size, optimizer, scheduler, augmentations, class balancing, and checkpoint-selection metric used for the submitted weights.
   - Add deterministic seeds and use a fixed, stratified train/validation/test split. Save the split manifest so every run evaluates the same images and no duplicate or derived image crosses a split boundary.
   Acceptance check: a second run can reconstruct the same splits and training configuration from committed metadata.
5. Freeze and audit the dataset.
   - Produce a manifest containing source dataset, source URL, original identifier/path, assigned class, checksum, and final split for every image.
   - Reconcile the conflicting presentation totals (~26,000 versus ~30,000) and class counts against the final manifest.
   - Deduplicate exact and near-duplicate images, inspect label noise, and document exclusions. Pay special attention to Tabby and Tiger_Cat because the project labels are visually/semantically ambiguous.
   - Preserve the official Oxford-IIIT Pet and Stanford Dogs citations, Kaggle dataset identifiers, TheCatAPI provenance where retained, the local Dalmatian source, and individual URLs for manually collected Tabby/Tiger_Cat images.
   - Convert the current data-loader sequence into one idempotent preprocessing command or script; append-only collection scripts alone are not reproducible.
   Acceptance check: the documented command rebuilds labels.csv and the 20-class directory/flat-image layout from the declared sources, with per-class totals matching the manifest.
6. Align preprocessing across training, inference, evaluation, and XAI.
   - Verify RGB/BGR conversions and use one shared transform definition: resize, 224 x 224 crop, tensor conversion, and normalization.
   - Decide whether zero-out masking remains in the final method by evaluating it as an ablation. The current detector implements masking, but adaptive bounding-box padding and classifier confidence-threshold rejection are not implemented and must not be claimed unless added and tested.
   - Ensure Grad-CAM uses the same detector crop and transform as inference when explaining final pipeline decisions.
   Acceptance check: a saved crop gives numerically consistent classifier logits in training validation, standalone evaluation, official inference, and XAI preprocessing paths.

Priority 2 - reproduce the empirical evidence required by the report:
7. Run a final locked evaluation and preserve machine-readable outputs.
   - Report end-to-end 21-class accuracy including reject, macro precision/recall/F1, per-class precision/recall/F1, and a 21 x 21 confusion matrix.
   - Separately report detector routing/localization performance, cat-head performance, dog-head performance, reject precision/recall, and complete-pipeline performance. Do not substitute classifier-only validation accuracy for end-to-end challenge accuracy.
   - State sample counts for every split and every metric; add uncertainty or repeated-seed results where practical.
   Acceptance check: a committed evaluation command regenerates all tables and figures from the frozen checkpoints and split manifest.
8. Reproduce or correct every numerical claim from the presentations before citing it as a final result.
   - Claims requiring evidence include 84.9%/0.829 for the pretrained 20-class model, 78.1%/0.752 for the from-scratch model, 95.3-95.4% cat accuracy, 87.08% dog accuracy, breed-level accuracies, detector accuracy, and the reported 0.21 s, 0.307 s, and 1.25 s latency values.
   - Identify which code path, checkpoint, split, and hardware produced each retained number. Remove or label any number that cannot be reproduced.
   - Profile cold-start time separately from warm per-image latency; include preprocessing, detector, classifier, and total timing with synchronization on CUDA, warm-up iterations, sample count, mean, standard deviation, and hardware.
   Acceptance check: every table cell in the report points to a saved result artifact and an exact run configuration.
9. Complete controlled ablations and negative-result analysis.
   - At minimum compare full-image versus detector crop, zero-out masking on/off, class weights on/off, and the final augmentation combination. If computationally feasible, retain the pretrained-versus-from-scratch comparison already discussed in the presentations.
   - Treat weighted random sampling, label smoothing, random erasing, warmup, and the 384-pixel experiments as notebook/alternate-track settings unless they are rerun in the final pipeline.
   - Treat Ranger as a proposed experiment only; the current code uses AdamW and no Ranger implementation is present.
   Acceptance check: each ablation changes one controlled factor, uses the same split, and reports both aggregate and per-class effects.
10. Perform a reproducible XAI analysis rather than only showing selected heatmaps.
    - Generate Grad-CAM from the final checkpoints for correctly classified, misclassified, ambiguous, masked, and reject examples.
    - Define the target layer and predicted/target class, preserve the unmodified input beside each overlay, and discuss both supporting and counterexample cases.
    - Avoid claiming that the model focuses on ears, eyes, and snouts unless the saved examples consistently support that interpretation; Grad-CAM is qualitative evidence, not proof of causal feature use.
    Acceptance check: a documented command regenerates every XAI figure used in the report.

Priority 3 - finish documentation, report, and release packaging:
11. Write the final paper in Final_Report using the provided 10-point, double-column template and keep the main text to at most eight pages excluding references.
    - Use the required scientific structure: Abstract, Introduction, Related Work, Method, Experiments (including empirical evaluation and XAI), and Conclusions.
    - Add the GitHub repository link and a detailed contribution list in the appendix.
    - Replace placeholder/member sections and unsupported draft claims with the verified content mapped in finalreport_draft.md.
    - Cite YOLOv6, EfficientNetV2, COCO, Oxford-IIIT Pet, Stanford Dogs, ImageNet where relevant, and Grad-CAM. Cite datasets and external images at their original sources.
    Acceptance check: the LaTeX build has no missing references/citations, broken figures, placeholder text, or unsupported numerical claims, and the page count satisfies the task.
12. Update README.md after the final workflow is frozen.
    - Provide clean setup instructions, supported Python/CUDA assumptions, detector dependency handling, exact data download/preprocessing commands, training and evaluation commands, expected checkpoint names, and archive layout.
    - Remove stale claims and clearly separate the final pipeline from historical experiments.
    Acceptance check: a teammate follows only the README on a clean checkout and reproduces inference plus at least one evaluation command.
13. Build and verify the release archive.
    - Include code, final detector/classifier weights, README, dependency specification, source/data manifest, split manifest, configuration, and evaluation artifacts required to reproduce the report.
    - Exclude caches, raw datasets where redistribution is not permitted, temporary files, debug outputs, backup scripts, secrets/API keys, and unrelated report build artifacts.
    - Test the exact archive after extraction in a new directory/environment, then tag the tested Git commit and record the commit hash in the report and manifest.
    Acceptance check: the extracted archive runs the official inference interface and regenerates the reported metrics without hidden local paths or untracked files.
